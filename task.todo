TODOs (✔/☐)

☐ How to check if the script is taking the right city (or town), once the community is created, not approved (MIGHT BE MANUAL). Once we approve, it has to be decided which is going to be the `osm_id`
☐ Create in an efficient way the docker image